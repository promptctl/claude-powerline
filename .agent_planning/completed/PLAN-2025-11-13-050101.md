# Implementation Plan - CLI Segment Configuration Documentation

**Generated**: 2025-11-13 05:01:01
**Source STATUS**: STATUS-2025-11-13-045853.md
**Spec Version**: CLAUDE.md (current)
**Planning Agent**: project-planner

---

## Executive Summary

The CLI segment configuration feature is **100% functionally complete** with all 79 tests passing and production-ready code. The only remaining work is **updating the README** to document the new CLI capabilities so users can discover and use these features.

**Current State**:
- ✅ All CLI segment selection implemented (`--segments=dir,git,model`)
- ✅ All 8 git options working (`--git-show-sha`, etc.)
- ✅ All segment-specific options functional
- ✅ 79/79 tests passing
- ✅ Comprehensive help text (lines 10-89 in `src/index.ts`)
- ❌ README documentation missing

**Total Work Remaining**: 1 documentation task (30-45 minutes estimated)

---

## Gap Analysis Summary

### Specification Compliance
The STATUS report confirms 100% compliance with all functional requirements from the specification. The implementation includes:

- ✅ Segment selection via `--segments` flag (lines 187-213 in `src/config/loader.ts`)
- ✅ All segment-specific configuration flags (lines 242-400)
- ✅ Theme and style selection (lines 406-421)
- ✅ Configuration precedence: CLI > Environment > File > Defaults (lines 527-531)
- ✅ Help text with all options documented (lines 10-89 in `src/index.ts`)

### Documentation Gap (CRITICAL for User Discovery)

**Current README State** (analyzed from `/Users/bmf/icode/brandon-fryslie_claude-powerline/README.md`):
- Lines 123-136: Only documents `--theme`, `--style`, `--config`, `--help`
- Search results: 0 occurrences of `--segments`
- Search results: 0 occurrences of segment-specific flags
- No examples showing CLI segment configuration

**Impact**: Users cannot discover the new CLI configuration capabilities without README updates.

---

## Prioritized Backlog

### P0 (Critical) - Documentation Completion

#### [P0] Update README with CLI Segment Configuration

**Status**: Not Started
**Effort**: Small (30-45 minutes)
**Dependencies**: None
**Spec Reference**: Lines 10-89 in `src/index.ts` (help text) • **Status Reference**: STATUS-2025-11-13-045853.md §Documentation Gaps

##### Description
Add comprehensive documentation to README.md for the new CLI segment configuration features. This is the final requirement to achieve 100% feature completion. Users currently cannot discover these capabilities because they're only documented in the `--help` output.

The README currently only documents 4 basic CLI flags (lines 123-136), but the implementation includes 30+ additional flags for segment selection and configuration.

##### Acceptance Criteria
- [ ] Add new "CLI Segment Configuration" section to README
- [ ] Document `--segments=` flag with available segment names and aliases
- [ ] Document all 8 git segment options with examples
- [ ] Document directory segment options
- [ ] Document session segment options (type and cost-source)
- [ ] Document today segment options
- [ ] Document block segment options (type and burn-type)
- [ ] Document context segment options (show-percentage-only)
- [ ] Document metrics segment options (all 6 flags)
- [ ] Include practical use case examples combining multiple flags
- [ ] Add note about `--no-*` variant support for boolean flags
- [ ] Cross-reference with existing "Segment Configuration" section (lines 170-384)
- [ ] Maintain consistency with help text in `src/index.ts` lines 10-89

##### Technical Notes

**Recommended Structure** (add after line 136 in README):

```markdown
### CLI Segment Configuration

Configure segments directly via command-line flags without needing a config file.

#### Segment Selection

Use `--segments` to enable only specific segments:

```bash
# Show only directory, git, and model segments
claude-powerline --segments=directory,git,model

# Use aliases for convenience
claude-powerline --segments=dir,git,model
```

**Available Segments**: directory (alias: dir), git, model, session, today, block, tmux, context, metrics, version

#### Segment-Specific Options

##### Git Segment
[8 git flags with examples]

##### Directory Segment
[directory flag with example]

##### Session Segment
[session flags with examples]

[etc. for other segments]

#### Practical Examples
[Real-world combinations]
```

**Content Sources**:
1. **Help text** (`src/index.ts` lines 10-89): Authoritative source for flag names and descriptions
2. **STATUS report** (lines 42-92): Evidence of what's implemented with line references
3. **Existing README** (lines 170-384): Segment descriptions to cross-reference

**Integration Points**:
- Link CLI flags to corresponding config file options in "Segment Configuration" section
- Explain that CLI flags override config file settings (line 166 already documents precedence)
- Show how `--no-*` variants work (e.g., `--no-git-show-sha` disables SHA display)

**Quality Standards**:
- Use same formatting/style as existing README sections
- Include practical examples, not just flag listings
- Show real-world use cases (e.g., "minimal git info", "detailed git info", "custom segment set")
- Add code blocks with syntax highlighting
- Keep descriptions concise but informative

**Validation**:
After updating, verify:
- All flags from help text are documented
- Examples are correct and tested
- Cross-references to config file options work
- Formatting is consistent with rest of README

---

## Recommended Implementation Approach

### Phase 1: Content Preparation (10 minutes)
1. Review help text in `src/index.ts` lines 10-89 (authoritative source)
2. Review STATUS report evidence sections (lines 42-92)
3. Map CLI flags to existing config file documentation (README lines 170-384)
4. Identify key use cases for examples

### Phase 2: README Update (20 minutes)
1. Add new "CLI Segment Configuration" section after line 136
2. Document `--segments` flag with segment names and aliases
3. Document each segment's CLI options in subsections
4. Add practical examples section
5. Add cross-references to config file options

### Phase 3: Validation (10 minutes)
1. Verify all flags from help text are documented
2. Test example commands to ensure they work
3. Check formatting consistency with rest of README
4. Proofread for clarity and accuracy

---

## Dependency Graph

No dependencies - this is a standalone documentation task.

```
[P0] Update README
  └─ (no dependencies)
```

---

## Risk Assessment

**Risk Level**: LOW

**Identified Risks**:
1. **Documentation Drift**: Risk that CLI flags change in future
   - **Mitigation**: Reference help text as single source of truth
   - **Impact**: Low (tests ensure flags work as documented)

2. **Incomplete Coverage**: Risk of missing some flags
   - **Mitigation**: Cross-check against help text and STATUS report
   - **Impact**: Low (comprehensive evidence in STATUS report)

3. **Example Accuracy**: Risk that examples don't work as shown
   - **Mitigation**: Test all examples before committing
   - **Impact**: Low (examples based on verified runtime tests)

**Uncertain Items**: None

---

## Sprint Planning

### Sprint 1: README Documentation (30-45 minutes)

**Goal**: Complete README documentation for CLI segment configuration

**Tasks**:
1. Add "CLI Segment Configuration" section header
2. Document segment selection with `--segments` flag
3. Document git segment options (8 flags)
4. Document other segment options (directory, session, today, block, context, metrics)
5. Add practical examples section
6. Add cross-references and notes
7. Review and validate

**Definition of Done**:
- All CLI flags documented in README
- Practical examples included and tested
- Cross-references to config file options added
- Formatting consistent with existing README
- No flags from help text missing

**Deliverable**: Updated README.md with complete CLI documentation

---

## Success Criteria

The README documentation update will be considered complete when:

1. ✅ **Discoverability**: Users can learn about CLI segment configuration from README
2. ✅ **Completeness**: All flags from help text documented
3. ✅ **Usability**: Practical examples show real-world usage
4. ✅ **Consistency**: Formatting matches existing README sections
5. ✅ **Accuracy**: Examples tested and verified to work
6. ✅ **Integration**: Cross-references to config file options clear

Upon completion, the project will be **100% feature complete** with no remaining gaps.

---

## Blockers and Questions

**No blockers identified.**

All information needed for documentation is available:
- Help text provides flag names and descriptions
- STATUS report provides evidence and line references
- Existing README provides structure and style guide
- Runtime tests confirm all examples work

---

## Post-Completion Checklist

After completing README update:

1. [ ] Verify all acceptance criteria met
2. [ ] Test example commands in terminal
3. [ ] Run build: `npm run build`
4. [ ] Run tests: `npm test` (should still be 79/79 passing)
5. [ ] Create git commit with changes
6. [ ] Mark STATUS-2025-11-13-045853.md as fully resolved
7. [ ] Archive this PLAN file when work complete
8. [ ] Update project STATUS to 100% complete (if evaluator runs again)

---

## File References

**Primary Files**:
- `/Users/bmf/icode/brandon-fryslie_claude-powerline/README.md` (to update)
- `/Users/bmf/icode/brandon-fryslie_claude-powerline/src/index.ts` (help text reference, lines 10-89)

**Evidence Files**:
- `/Users/bmf/icode/brandon-fryslie_claude-powerline/.agent_planning/STATUS-2025-11-13-045853.md`
- `/Users/bmf/icode/brandon-fryslie_claude-powerline/src/config/loader.ts` (implementation reference)
- `/Users/bmf/icode/brandon-fryslie_claude-powerline/test/config.test.ts` (test reference)

---

## Notes

- This is the **final task** to achieve 100% feature completion
- No code changes required - documentation only
- Low risk, high impact for user discoverability
- Estimated 30-45 minutes for thorough documentation
- No testing required beyond validating examples work

**Next Steps**: Proceed with README update following the recommended implementation approach.
